import React, { Component } from 'react';
import { View } from 'react-native';

export default () => (
  <View
    style={{
      height: 5,
      backgroundColor: 'pink',
      marginTop: 10,
      marginBottom: 10,
    }}
  />
);
